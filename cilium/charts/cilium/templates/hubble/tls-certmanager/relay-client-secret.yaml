---
# Source: cilium/charts/cilium/templates/hubble/tls-certmanager/relay-client-secret.yaml
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: hubble-relay-client-certs
  namespace: cluster-core
spec:
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: hubble-ca-issuer
  secretName: hubble-relay-client-certs
  commonName: "*.hubble-relay.cilium.io"
  dnsNames:
  - "*.hubble-relay.cilium.io"
  duration: 26280h0m0s
  privateKey:
    rotationPolicy: Always
  isCA: false
  usages:
    - signing
    - key encipherment
    - client auth
